import 'package:flutter/widgets.dart';

class Addcroppage extends StatelessWidget {
  const Addcroppage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Add Crop");
  }
}